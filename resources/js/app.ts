import {createApp,ref,onMounted,nextTick} from 'vue';import axios from'axios';import*as echarts from'echarts';import'../css/app.css';
axios.defaults.headers.common['X-Requested-With']='XMLHttpRequest';axios.defaults.headers.common['X-CSRF-TOKEN']=(document.querySelector('meta[name=csrf-token]')as HTMLMetaElement).content;
const App={setup(){const nav=['Overview','People','Groups','Network','Imports','Exports','Settings'],page=ref('Overview'),loading=ref(false),error=ref(''),overview=ref<any>({top_people:[]}),people=ref<any[]>([]),imports=ref<any[]>([]),groups=ref<any[]>([]),group=ref<any>(null),candidates=ref<any[]>([]),ownerImport=ref<any>(null);
async function load(){loading.value=true;error.value='';try{if(page.value==='Overview')overview.value=(await axios.get('/api/analytics/overview')).data.data;if(page.value==='People')people.value=(await axios.get('/api/analytics/people')).data.data.data;if(page.value==='Imports')imports.value=(await axios.get('/api/imports')).data.data.data;if(page.value==='Groups')groups.value=(await axios.get('/api/analytics/groups')).data.data.data;if(page.value==='Network'){const d=(await axios.get('/api/analytics/network')).data.data;await nextTick();echarts.init(document.getElementById('network')!).setOption({tooltip:{},series:[{type:'graph',layout:'force',roam:true,label:{show:true,color:'#dce9f2'},data:d.nodes.map((n:any)=>({...n,symbolSize:Math.max(18,Math.log(n.value+1)*10)})),links:d.links,lineStyle:{color:'#31d0aa'},force:{repulsion:180}}]})}}catch(e:any){error.value=e.response?.data?.message||'Unable to load this view.'}finally{loading.value=false;}}
async function upload(e:any){loading.value=true;try{await axios.post('/api/imports',new FormData(e.target));await load()}catch(x:any){error.value=x.response?.data?.message||'Upload failed.'}finally{loading.value=false}}async function owner(i:any){ownerImport.value=i;candidates.value=(await axios.get(`/api/imports/${i.id}/owner-candidates`)).data.data}async function resolve(id:string){await axios.post(`/api/imports/${ownerImport.value.id}/resolve-owner`,{social_person_id:id});ownerImport.value=null;candidates.value=[];await load()}async function cancel(i:any){await axios.post(`/api/imports/${i.id}/cancel`);await load()}async function retry(i:any){await axios.post(`/api/imports/${i.id}/retry`);await load()}async function openGroup(g:any){group.value=(await axios.get(`/api/analytics/groups/${g.id}`)).data.data}async function makeExport(format:string){await axios.post('/api/exports',{format});alert(`${format.toUpperCase()} export queued. Generated files expire after seven days.`)}async function erase(){if(confirm('Permanently delete all of your analytics data and private exports?')){await axios.delete('/api/analytics/data');await load()}}onMounted(load);return{nav,page,loading,error,overview,people,imports,groups,group,candidates,ownerImport,load,upload,owner,resolve,cancel,retry,openGroup,makeExport,erase}},
template:`<div class="shell"><aside class="side"><div class="brand">Interaction <b>Atlas</b></div><nav class="nav"><button v-for="n in nav" :class="{active:page===n}" @click="page=n;group=null;load()">{{n}}</button></nav></aside><main class="main"><div class="eyebrow">Personal analytics · privacy first</div><h1 class="title">{{page}}</h1><p v-if="loading" class="muted">Loading…</p><p v-if="error" class="error">{{error}}</p>
<section v-if="page==='Overview'" class="grid"><div class="card"><div class="muted">People</div><div class="metric">{{overview.people||0}}</div></div><div class="card"><div class="muted">Messages</div><div class="metric">{{overview.total_messages||0}}</div></div><div class="card"><div class="muted">Sent / received</div><div class="metric">{{overview.messages_sent||0}} / {{overview.messages_received||0}}</div></div><div class="card"><div class="muted">Active · 30 days</div><div class="metric">{{overview.active_people_30_days||0}}</div></div><div class="card wide"><h3>Top interactions</h3><table><tr v-for="e in overview.top_people"><td>{{e.person.display_name}}</td><td>{{e.messages_sent+e.messages_received}} messages</td><td class="score">{{e.overall_score}}</td></tr></table><p v-if="!overview.top_people?.length" class="muted">Upload an export to begin.</p></div><div class="card wide"><h3>Data coverage matters</h3><p class="muted">Unavailable Facebook categories are excluded from scores—not treated as zero.</p></div></section>
<div v-if="page==='People'" class="card"><table><tr><th>Person</th><th>Score</th><th>Messages</th><th>Active days</th><th>Sessions</th><th>Mutuality</th><th>Trend</th></tr><tr v-for="e in people"><td>{{e.person.display_name}}</td><td class="score">{{e.overall_score}}</td><td>{{e.messages_sent+e.messages_received}}</td><td>{{e.active_days}}</td><td>{{e.session_count}}</td><td>{{e.mutuality_score}}</td><td><span class="pill">{{e.trend}}</span></td></tr></table></div>
<template v-if="page==='Imports'"><form class="drop" @submit.prevent="upload"><h2>Upload your Facebook export</h2><p class="muted">ZIP or JSON · message content privacy is on by default</p><input type="file" name="archive" accept=".zip,.json" required><input type="hidden" name="privacy_mode" value="1"><button class="primary" :disabled="loading">Upload export</button></form><div class="card" style="margin-top:18px"><table><tr v-for="i in imports"><td><b>{{i.original_filename}}</b><div class="muted">{{i.processed_files}}/{{i.total_files}} files · {{i.records_processed}} imported · {{i.records_skipped}} skipped</div></td><td>{{i.status}}<div class="bar"><i :style="{width:i.progress+'%'}"></i></div></td><td><button v-if="i.owner_resolution_status==='needs_confirmation'" class="primary" @click="owner(i)">Identify me</button><button v-if="['queued','extracting','scanning','parsing'].includes(i.status)" class="danger" @click="cancel(i)">Cancel</button><button v-if="['failed','cancelled'].includes(i.status)" class="primary" @click="retry(i)">Retry</button></td></tr></table></div><div v-if="ownerImport" class="card" style="margin-top:18px"><h2>Which participant represents you?</h2><p class="muted">We could not identify the owner confidently. Select your identity; analytics will be recalculated.</p><button v-for="c in candidates" class="primary" style="margin:5px" @click="resolve(c.id)">{{c.display_name}} · {{c.confidence}}% confidence</button></div></template>
<template v-if="page==='Groups'"><div v-if="!group" class="card"><table><tr v-for="g in groups" @click="openGroup(g)" style="cursor:pointer"><td>{{g.title}}</td><td>{{g.participant_count}} participants</td><td>{{g.message_count}} messages</td></tr></table></div><div v-else class="card"><button @click="group=null">← Groups</button><h2>{{group.group.title}}</h2><table><tr><th>Participant</th><th>Messages</th><th>Share</th><th>Active days</th></tr><tr v-for="p in group.participants.data"><td>{{p.display_name}}</td><td>{{p.messages}}</td><td>{{p.share_of_messages}}%</td><td>{{p.active_days}}</td></tr></table></div></template><div v-if="page==='Network'" id="network" class="card chart"></div><div v-if="page==='Exports'" class="card"><h2>Private analytics workbook</h2><p class="muted">XLSX includes eight analytics sheets without raw message text. CSV is available for quick use.</p><button class="primary" @click="makeExport('xlsx')">Export XLSX</button> <button @click="makeExport('csv')">Export CSV</button></div><div v-if="page==='Settings'" class="card"><h2>Privacy controls</h2><p>Message content is discarded by default. Generated exports expire after seven days.</p><button class="danger" @click="erase">Delete all analytics data</button></div></main></div>`};createApp(App).mount('#app');
